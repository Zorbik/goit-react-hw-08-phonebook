import { Box, RegistrationForm } from '../../components';

const Registration = () => {
  return (
    <Box display="flex" justifyContent="center" my={6}>
      <RegistrationForm />
    </Box>
  );
};

export default Registration;
